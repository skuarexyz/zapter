{
  "name": "zapter",
  "version": "0.0.1",
  "description": "Actually good chatting protocol + built-in client",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.1.2"
  },
  "scripts": {
    "start": "node index.js"
  }
}
